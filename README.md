I chose to code in Java for the project.

**** When running the program, some test cases, if copied and pasted, that have "" will throw an error. For some reason the copy and paste quotes aren't in a proper format and will just stop the program.****

Lexer:
In order to run the program in the terminal type javac Main.java Lexer.java Token.java Parser.java SemanticAnalyzer.java CodeGenerator.java ASTBuilder.java
then javac Main test.txt

Project Part 2: is saved in the Main Branch of Github in the folder there is another folder that has all of the contents. I also created a separate branch for it (not too familiar with Github branches). To run the program it'll be the same commands as the regular project.

Project Part 2: I simply put into ChatGPT and asked it to take my code and follow the guidelines from the part 2 pdf. It actually worked on the first try. It generates new files with the changes that were asked in the pdf. I tried it again a second time and it completely messed up the code and didn't do what was asked. the third time was the same. I gave it the same files and the same pdf and asked it to do what was being asked in the pdf, just like the first time, and it didn't do what was asked. The use of AI is nice but I believe that the constant use of it for coding will mess things up and make it harder to work on. For the files that actually worked, it completed the tasks, in a crude way but it was completed. I didn't like that it was making its own files every time that the code was compiled. That on its own made navigating very difficult. All in all, if you don't give the AI exact instructions on how to format and what you want in precise detail, it'll do whatever it wants and it'll turn out terrible.
